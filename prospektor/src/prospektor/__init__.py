"""prospektor — поиск бизнесов и скоринг цифровых разрывов.

Модуль изолирован от основного проекта segregator: ноль импортов через границу
в обе стороны, своё хранилище под ``data/``, свои настройки прав в ``.claude/``.
Проверяется тестом ``tests/test_isolation.py``, а не договорённостью.
"""

__version__ = "0.1.0"
