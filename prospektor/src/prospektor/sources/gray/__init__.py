"""«Серые» источники: технически доступны, но нарушают ToS площадки.

Сюда попадают разбор публичных страниц ``/maps/place/`` и сторонние SERP/Maps API.
Риск здесь договорный, а не уголовный: суды США с 2017 года последовательно не
видят в сборе публичных данных нарушения CFAA, но пункт «No Scraping» в Google
Maps Platform ToS никуда не делся — значит блокировки, отзыв ключей и
cease-and-desist вполне реальны.

Поэтому конструкция такая: интерфейс тот же, папка отдельная, включение — двойным
замком (``PROSPEKTOR_ALLOW_GRAY=1`` плюс ``--legal-mode gray``), а факт
использования пишется в манифест прогона. Реализаций здесь пока нет намеренно:
каркас готов, решение включать — за человеком.
"""

from __future__ import annotations

from prospektor.sources.base import SourceAdapter

GRAY_SOURCES: dict[str, type[SourceAdapter]] = {}

__all__ = ["GRAY_SOURCES"]
