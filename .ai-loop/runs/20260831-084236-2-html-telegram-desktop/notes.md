# Заметки прогона — замечено, не трогаю в этом прогоне

- **`ledger.jsonl`: фаза BUILD прошлой сессии записана под `run_id: "R"`**
  (литеральный placeholder), а не под `20260831-084236-2-html-telegram-desktop`.
  Плюс два чужих прогона Why_Ai (seq 8–19). Ledger append-only, чистит человек.
  Для REVIEW/SHIP: доказательство ревью этого прогона — записи с правильным
  run_id, начиная с DEFINE (seq 24+).

- **`html_reader.resolve_chat_id` vs `export_reader.read_chat_id`** — разные
  имена у аналогичных функций. `ingest.export` их сглаживает. Переименовывать
  `resolve_chat_id` не стал: файл уже одобрен, а лишняя правка в диффе, за
  которой не следят, — это то, чего просит не делать loop-build.

- **`probe/02_html_structure.py` уже писался в прошлой сессии** и попал в
  кумулятивный дифф этого прогона (зона R). Не мой код; проверяется
  boundary-guard вместе с остальным.

- **boundary-guard: вердикт «чисто».** Сети на пути документа нет, содержимое
  не логируется, резолв путей защищён, фикстуры синтетические и
  зарегистрированы, probe обезличен. Два некритичных замечания:
  1. HTML-фикстуры не несли маркер синтетичности в первой строке (бинарные
     заглушки несут) — **исправлено**: добавлен `<!-- SYNTHETIC fixture ... -->`
     первой строкой в `messages.html`, `messages2.html`, `tracker_widget.html`.
  2. `probe/mask_path()` сохраняет полное имя каталога (`files/2025/klient_x`).
     Для стандартного экспорта Telegram каталоги фиксированы, утечки нет.
     **Не трогаю** — probe в зоне R, из прошлой сессии; кандидат в отдельный
     мелкий прогон, если реальный экспорт покажет вложенные подкаталоги.

- **Тесты диспетчера и backfill-html** (A1–A7) — в VERIFY, не в BUILD.
  Smoke-проверка руками (не в репозитории) уже сошлась с числами spec:
  messages 7 / blobs 4 / attachments 5 / missing 1, повторный прогон — нули.
